# Sentinel 7.0

MMO игра для Telegram на Deno и MongoDB.

## Быстрый старт

1. Установите Deno: https://deno.land
2. Запустите MongoDB: `docker run -d -p 27017:27017 --name mongodb mongo:5`
3. Скопируйте .env.example в .env и укажите BOT_TOKEN
4. Запустите: `deno task start`
